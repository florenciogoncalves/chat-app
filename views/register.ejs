<%- include('./partials/header.ejs') %>
<div class="m-auto h-100 d-flex justify-content-center align-items-center">
  <div class="card text-start col-4">
    <div class="card-body px-5 py-4">
      <h4 class="card-title mb-4 mt-2">Cadastre-se</h4>
      <div class="d-flex flex-column">
        <form action="/authenticate/register" method="post">
          <div class="mb-3">
            <label for="name" class="form-label">Nome</label>
            <input
              type="text"
              class="form-control"
              name="name"
              id="name"
              placeholder="Seu nome"
              required
            />
          </div>
          <div class="mb-3">
            <label for="email" class="form-label">Email</label>
            <input
              type="email"
              class="form-control"
              name="email"
              id="email"
              placeholder="Seu email"
              required
            />
          </div>
          <div class="mb-3">
            <label for="password" class="form-label">Senha</label>
            <input
              type="text"
              class="form-control"
              name="password"
              id="password"
              placeholder="********"
            />
          </div>
          <button type="submit" class="btn btn-success col-12 py-2 mt-3">
            Cadastrar
          </button>
          <div class="mt-3 text-center">
            Já possui uma conta? <a href="/login">Entre</a>
          </div>
        </form>
      </div>
    </div>
  </div>
</div>
<%- include('./partials/footer.ejs') %>
